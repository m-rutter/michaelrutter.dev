name: CI

on: [push]

jobs:
  build:
    name: shalzz/zola-deploy-action
    runs-on: ubuntu-latest

    steps:
    - uses: shalzz/zola-deploy-action@v0.9.0
    - name: shalzz/zola-deploy-action
      env:
        PAGES_BRANCH: master
        TOKEN: ${{ secrets.TOKEN }}
